import React from "react";
import { data } from "../../data";
import ReelList from "../../Reels/ReelsList";

export default function ReelScreen({ navigation }) {
  return <ReelList navigation={navigation} />;
}
